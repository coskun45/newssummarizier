backend/.env
backend/.venv
.vscode
frontend/node_modules
**/__pycache__
**/*.pyc
**/*.pyo
.env
.env.*
!.env.example
backend/app/__pycache__

# Host data dir (deploys bind-mount ./data into the container: generated bulletins).
# Legacy SQLite files: the app uses PostgreSQL now, but migrate_sqlite_to_postgres.py / deploy.sh
# still read an old news_summary.db if one exists — never commit it.
/data/
backend/news_summary.db
backend/news_summary.db-shm
backend/news_summary.db-wal
backend/bulletin_reports/



# Claude Code — personal settings, never in git
.claude/settings.local.json
